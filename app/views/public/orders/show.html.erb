<p>注文履歴詳細</p>

 <table class="table">
  <p>注文情報</p>
            <thead>
                <tr>
                    <th>注文日</th>
                    <th>配送先</th>
                    <th>支払方法</th>
                    <th>ステータス</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><%= order.created_at.strftime('%Y/%m/%d') %></td>
                    <td>
                      <%= order_item.post_code %>
                      <%= order_item.address %>
                      <%= order.item.name %>
                    </td>
                    <td><%= order_item.name %></td>
                    <td><%= order_item.payent %></td>
                    <td><%= order_item.status %></td>
                    <td><%= link_to "表示する", public_order_path(order), method: :delete, class: "btn btn-primary" %>
                    </td>
                </tr>
            </tbody>
            <p>請求情報</p>
             <tr>
                    <th>商品合計</th>
                    <th>配送料</th>
                    <th>後請求額</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><%= order_item.name %></td>
                    <td><%= @postage %></td>
                    <td><%= order_item.status %></td>
                </tr>
            
            <P>注文内容</P>
                <tr>
                    <th>商品</th>
                    <th>単価（税込）</th>
                    <th>個数</th>
                    <th>小計</th>
                </tr>
             <%= @order_item, each do |order_item| %>
                <tr>
                    <td><%= order_item.name %></td>
                    <td><%= oart_item.item.price.to_s(:delimited) %></td>
                    <td><%= order_item.amount %></td>
                    <td><%= order_item.item.price*order_item.amount.to_s(:delimited) %></td>
                </tr>
            <% end %>    
  </table>